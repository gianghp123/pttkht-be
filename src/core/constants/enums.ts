export enum PermissionType {
  READ = 'read',
  WRITE = 'write',
  SHARE = 'share',
}

export enum Role {
  User = 'user',
  Admin = 'admin',
}

export enum PermissionLevel {
  READ = 1,
  WRITE = 2,
  SHARE = 3,
}
